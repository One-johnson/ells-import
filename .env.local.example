# Run `npx convex dev` to log in, create a deployment, and write this to .env.local
NEXT_PUBLIC_CONVEX_URL=

# Paystack (optional — add when implementing hosted checkout; keep checkout_payment_channel on whatsapp until then)
# NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=
# Convex dashboard → Settings → Environment variables:
# PAYSTACK_SECRET_KEY=
#
# OpenAI (admin “Generate with AI” product descriptions):
# OPENAI_API_KEY=sk-...
# Optional — default gpt-4o-mini:
# OPENAI_MODEL=gpt-4o-mini
